<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.oes.mapper.MultiQuestionMapper">

    <select id="findByIdAndType" resultType="com.test.oes.entity.MultiQuestion">
        select *
        from multi_question
        where questionId in (
            select questionId from paper_manage where questionType = 1 and paperId = #{PaperId}
        )
    </select>

    <select id="findAll" resultType="com.test.oes.entity.MultiQuestion">
        select * from multi_question
    </select>

    <select id="findOnlyQuestionId" resultType="com.test.oes.entity.MultiQuestion">
        select questionId from multi_question order by questionId desc limit 1
    </select>

    <insert id="add" useGeneratedKeys="true" keyProperty="questionId">
        insert into multi_question(subject, question, answerA, answerB, answerC, answerD, rightAnswer, analysis, section, level)
        values(#{subject}, #{question}, #{answerA}, #{answerB}, #{answerC}, #{answerD}, #{rightAnswer}, #{analysis}, #{section}, #{level})
    </insert>

    <select id="findIdsBySubject" resultType="int">
        select questionId from multi_question where subject = #{subject} order by questionId asc
    </select>

    <update id="edit">
        update multi_question
        set subject = #{subject},
            question = #{question},
            answerA = #{answerA},
            answerB = #{answerB},
            answerC = #{answerC},
            answerD = #{answerD},
            rightAnswer = #{rightAnswer},
            analysis = #{analysis},
            section = #{section},
            level = #{level}
        where questionId = #{questionId}
    </update>

    <select id="findQuestionBySubject" resultType="com.test.oes.entity.MultiQuestion">
        select * from multi_question where subject = #{subject}
    </select>

    <select id="findByQuestionId" resultType="com.test.oes.entity.MultiQuestion">
        select * from multi_question where questionId = #{questionId}
    </select>

    <select id="findByQuestionIds" resultType="com.test.oes.entity.MultiQuestion">
        select * from multi_question where questionId in
        <foreach collection="questionIds" item="questionId" open="(" separator="," close=")">
            #{questionId}
        </foreach>
    </select>

    <delete id="deleteByQuestionId">
        delete from multi_question where questionId = #{questionId}
    </delete>
</mapper>
