<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.oes.mapper.AsyncEventOutboxMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO async_event_outbox(
            event_id, event_type, aggregate_type, aggregate_id, routing_key, payload_json,
            status, retry_count, next_retry_at, published_at, last_error, created_at
        ) VALUES (
            #{eventId}, #{eventType}, #{aggregateType}, #{aggregateId}, #{routingKey}, #{payloadJson},
            #{status}, #{retryCount}, #{nextRetryAt}, #{publishedAt}, #{lastError}, #{createdAt}
        )
    </insert>

    <select id="findByEventId" resultType="com.test.oes.entity.AsyncEventOutbox">
        SELECT id,
               event_id as eventId,
               event_type as eventType,
               aggregate_type as aggregateType,
               aggregate_id as aggregateId,
               routing_key as routingKey,
               payload_json as payloadJson,
               status,
               retry_count as retryCount,
               next_retry_at as nextRetryAt,
               published_at as publishedAt,
               last_error as lastError,
               created_at as createdAt
        FROM async_event_outbox
        WHERE event_id = #{eventId}
        LIMIT 1
    </select>

    <select id="lockBatchForPublish" resultType="com.test.oes.entity.AsyncEventOutbox">
        SELECT id,
               event_id as eventId,
               event_type as eventType,
               aggregate_type as aggregateType,
               aggregate_id as aggregateId,
               routing_key as routingKey,
               payload_json as payloadJson,
               status,
               retry_count as retryCount,
               next_retry_at as nextRetryAt,
               published_at as publishedAt,
               last_error as lastError,
               created_at as createdAt
        FROM async_event_outbox
        WHERE status IN ('NEW', 'RETRY')
          AND (next_retry_at IS NULL OR next_retry_at &lt;= #{now})
        ORDER BY id
        LIMIT #{batchSize}
        FOR UPDATE SKIP LOCKED
    </select>

    <update id="updateStatusBatch">
        UPDATE async_event_outbox
        SET status = #{status}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="markPublished">
        UPDATE async_event_outbox
        SET status = 'PUBLISHED',
            published_at = #{publishedAt},
            last_error = NULL
        WHERE id = #{id}
    </update>

    <update id="markRetryOrFailed">
        UPDATE async_event_outbox
        SET status = #{status},
            retry_count = #{retryCount},
            next_retry_at = #{nextRetryAt},
            last_error = #{lastError}
        WHERE id = #{id}
    </update>

    <select id="countPending" resultType="long">
        SELECT COUNT(*)
        FROM async_event_outbox
        WHERE status IN ('NEW', 'RETRY', 'PROCESSING')
    </select>
</mapper>
