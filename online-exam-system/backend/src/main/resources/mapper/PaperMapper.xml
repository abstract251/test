<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.oes.mapper.PaperMapper">

    <select id="findAll" resultType="com.test.oes.entity.PaperManage">
        select paperId, questionType, questionId from paper_manage
    </select>

    <select id="findById" resultType="com.test.oes.entity.PaperManage">
        select paperId, questionType, questionId from paper_manage where paperId = #{paperId}
    </select>

    <select id="countByPaperId" resultType="int">
        select count(*) from paper_manage where paperId = #{paperId}
    </select>

    <select id="countQuestionsByPaperIds" resultType="map">
        select paperId as paperId, count(*) as questionCount
        from paper_manage
        where paperId in
        <foreach collection="paperIds" item="paperId" open="(" separator="," close=")">
            #{paperId}
        </foreach>
        group by paperId
    </select>

    <select id="countQuestionsGroupedByType" resultType="map">
        select questionType as questionType, count(*) as questionCount
        from paper_manage
        where paperId = #{paperId}
        group by questionType
    </select>

    <insert id="add">
        insert into paper_manage(paperId, questionType, questionId)
        values (#{paperId}, #{questionType}, #{questionId})
    </insert>

    <insert id="batchInsert">
        insert into paper_manage(paperId, questionType, questionId) values
        <foreach collection="rows" item="row" separator=",">
            (#{row.paperId}, #{row.questionType}, #{row.questionId})
        </foreach>
    </insert>

    <delete id="delete">
        delete from paper_manage
        where paperId = #{paperId}
          and questionType = #{type}
          and questionId = #{questionId}
    </delete>

    <select id="findPaperIdsByQuestion" resultType="int">
        select distinct paperId from paper_manage where questionType = #{questionType} and questionId = #{questionId}
    </select>

    <delete id="deleteByQuestion">
        delete from paper_manage where questionType = #{questionType} and questionId = #{questionId}
    </delete>

    <delete id="deleteByPaperId">
        DELETE FROM paper_manage WHERE paperId = #{paperId}
    </delete>
</mapper>
