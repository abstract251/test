<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.oes.mapper.ExamSharedSnapshotMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO exam_shared_snapshot(exam_code, paper_id, created_at)
        VALUES (#{examCode}, #{paperId}, #{createdAt})
    </insert>

    <select id="countByExamCode" resultType="int">
        SELECT COUNT(*) FROM exam_shared_snapshot WHERE exam_code = #{examCode}
    </select>

    <select id="findExistingExamCodes" resultType="int">
        SELECT DISTINCT exam_code
        FROM exam_shared_snapshot
        WHERE exam_code IN
        <foreach collection="examCodes" item="examCode" open="(" separator="," close=")">
            #{examCode}
        </foreach>
    </select>
</mapper>
